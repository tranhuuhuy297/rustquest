/* Realm 3 — Structs & Enums. Appends one module to window.RUSTQUEST.modules. */
window.RUSTQUEST.modules.push({
  id: "structs-enums",
  n: 3,
  title: "Structs & Enums",
  subtitle: "Model your domain with custom types and pattern matching.",
  color: "#D98B3A",
  icon: "blocks",
  lessons: [
    {
      id: "structs",
      title: "Defining Structs",
      tag: "Bundle related data into a named type",
      min: 10,
      xp: 150,
      blocks: [
        { t: "text", html: "A <strong>struct</strong> groups related values under a single named type. Think of it as a custom data record — like a row in a table where each column has a name and a type." },
        { t: "code", caption: "Defining and instantiating a struct", code: "struct Point {\n    x: f64,\n    y: f64,\n}\n\nfn main() {\n    let p = Point { x: 3.0, y: 4.0 };\n    println!(\"({}, {})\", p.x, p.y);\n}" },
        { t: "text", html: "Access fields with dot notation (<code>p.x</code>). To update a field the whole struct binding must be <code>mut</code> — there's no per-field mutability." },
        { t: "callout", kind: "tip", html: "<strong>Struct update syntax</strong> lets you create a new instance while copying most fields from an existing one. Use <code>..other</code> as the last entry:\n<code>let p2 = Point { x: 1.0, ..p };</code>" },
        { t: "play", prompt: "Run to see field access and struct update syntax in action.", code: "struct Point {\n    x: f64,\n    y: f64,\n}\n\nfn main() {\n    let p1 = Point { x: 3.0, y: 4.0 };\n    let p2 = Point { x: 1.0, ..p1 };\n    println!(\"p1: ({}, {})\", p1.x, p1.y);\n    println!(\"p2: ({}, {})\", p2.x, p2.y);\n}", output: "p1: (3, 4)\np2: (1, 4)", need: ["struct", "Point"] },
        { t: "callout", kind: "note", html: "Rust also has <em>tuple structs</em> (<code>struct Meters(f64);</code>) and the <em>unit struct</em> (<code>struct Marker;</code>). Tuple structs are handy when the field names would add no clarity." },
        { t: "fib", prompt: "Complete the field access to read the `y` field of a `Point` called `pt`.", before: "let y = pt.", blank: "y", after: ";", answer: "y", hint: "Dot notation: structname.fieldname" },
      ],
      quiz: [
        { q: "How do you access the `name` field of a struct variable `u`?", opts: ["u[name]", "u->name", "u.name", "u::name"], a: 2, why: "Dot notation accesses struct fields: `u.name`." },
        { q: "What does struct update syntax `..other` do?", opts: ["Deletes the other struct", "Copies remaining fields from `other`", "Converts `other` to a tuple", "Makes all fields mutable"], a: 1, why: "`..other` fills in any fields you didn't explicitly set by copying them from `other`." },
        { q: "To mutate a field inside a struct, what must you declare?", opts: ["Only that field as `mut`", "The entire struct binding as `mut`", "Nothing — fields are always mutable", "A `mut` keyword inside the struct definition"], a: 1, why: "Mutability is all-or-nothing at the binding level — `let mut s = MyStruct { ... }`." },
      ],
      takeaways: ["Structs bundle named fields into a single type.", "Access fields with dot notation; the whole binding must be `mut` to mutate.", "Struct update syntax `..other` copies unspecified fields from an existing instance."],
    },
    {
      id: "methods",
      title: "Methods & impl",
      tag: "Attach behavior to your types",
      min: 10,
      xp: 150,
      blocks: [
        { t: "text", html: "You attach behavior to a struct using an <code>impl</code> block. Functions inside <code>impl</code> that take <code>&amp;self</code> as the first parameter are called <strong>methods</strong> — you call them with dot notation." },
        { t: "code", caption: "impl block with a method", code: "struct Circle {\n    radius: f64,\n}\n\nimpl Circle {\n    fn area(&self) -> f64 {\n        std::f64::consts::PI * self.radius * self.radius\n    }\n}" },
        { t: "text", html: "Functions inside <code>impl</code> that do <em>not</em> take <code>&amp;self</code> are called <strong>associated functions</strong>. They're called with <code>::</code> syntax (like <code>String::new()</code>) and are commonly used as constructors." },
        { t: "play", prompt: "Run to see both a constructor (`new`) and a method (`area`) in action.", code: "struct Circle {\n    radius: f64,\n}\n\nimpl Circle {\n    fn new(radius: f64) -> Circle {\n        Circle { radius }\n    }\n    fn area(&self) -> f64 {\n        3.14159 * self.radius * self.radius\n    }\n}\n\nfn main() {\n    let c = Circle::new(2.0);\n    println!(\"area = {:.2}\", c.area());\n}", output: "area = 12.57", need: ["impl", "Circle", "&self", "fn new"] },
        { t: "callout", kind: "tip", html: "When a method only reads data, take <code>&amp;self</code>. To mutate fields, take <code>&amp;mut self</code>. To consume the value entirely, take <code>self</code> (rare)." },
        { t: "fib", prompt: "Complete the method signature so it borrows `self` immutably.", before: "fn describe(", blank: "&self", after: ") -> String { format!(\"r={}\", self.radius) }", answer: "&self", hint: "The first parameter of a non-consuming, non-mutating method." },
        { t: "callout", kind: "note", html: "You can have multiple <code>impl</code> blocks for the same struct — Rust merges them. This is useful when organizing trait implementations in larger files." },
      ],
      quiz: [
        { q: "What is the difference between a method and an associated function in `impl`?", opts: ["Associated functions are faster", "Methods take `&self`; associated functions do not", "There is no difference", "Associated functions must return `Self`"], a: 1, why: "Methods receive `&self` (or `&mut self`/`self`) as first param; associated functions don't touch an instance." },
        { q: "How do you call an associated function `new` on a struct `Rect`?", opts: ["Rect.new()", "Rect->new()", "Rect::new()", "new::Rect()"], a: 2, why: "Associated functions are namespaced to the type with `::`, e.g. `Rect::new()`." },
      ],
      takeaways: ["Methods live in `impl` blocks and take `&self` as first parameter.", "Associated functions use `::` syntax and are often constructors.", "`&self` borrows immutably; `&mut self` borrows mutably."],
    },
    {
      id: "enums",
      title: "Enums",
      tag: "One type, several shapes",
      min: 11,
      xp: 160,
      blocks: [
        { t: "text", html: "An <strong>enum</strong> defines a type that can be one of several named <em>variants</em>. Unlike enums in many languages, Rust variants can carry data — making them closer to algebraic data types." },
        { t: "code", caption: "Enum with data variants", code: "enum Shape {\n    Circle(f64),          // radius\n    Rectangle(f64, f64),  // width, height\n    Triangle { base: f64, height: f64 },\n}" },
        { t: "text", html: "To create a value, use the variant name: <code>Shape::Circle(3.0)</code>. You can also write <code>impl</code> blocks for enums, just like structs." },
        { t: "play", prompt: "Run to see enum variants printed with a helper method.", code: 'enum Direction {\n    North,\n    South,\n    East,\n    West,\n}\n\nimpl Direction {\n    fn label(&self) -> &str {\n        match self {\n            Direction::North => "North",\n            Direction::South => "South",\n            Direction::East  => "East",\n            Direction::West  => "West",\n        }\n    }\n}\n\nfn main() {\n    let d = Direction::North;\n    println!("Heading: {}", d.label());\n}', output: "Heading: North", need: ["enum", "Direction", "impl", "match"] },
        { t: "callout", kind: "tip", html: "Enums in Rust are <em>sum types</em>: a value of that type is exactly one of the variants — never two at once. This is enforced by the type system and makes <code>match</code> exhaustiveness checks possible." },
        { t: "fib", prompt: "Create a `Color` enum variant called `Red` with no data.", before: "enum Color { ", blank: "Red", after: ", Green, Blue }", answer: "Red", hint: "Just the variant name — simple variants carry no data." },
        { t: "callout", kind: "note", html: "Rust's standard library uses enums heavily. <code>Option&lt;T&gt;</code> and <code>Result&lt;T, E&gt;</code> are both enums, which you'll meet very soon." },
      ],
      quiz: [
        { q: "Can Rust enum variants carry data?", opts: ["No, they are just names", "Yes, each variant can carry different data", "Only if the enum derives Debug", "Only unit variants are allowed"], a: 1, why: "Rust enum variants can be unit, tuple-like (unnamed fields), or struct-like (named fields)." },
        { q: "How do you construct a `Color::Blue` value?", opts: ["Color.Blue", "new Color(Blue)", "Color::Blue", "Color { Blue }"], a: 2, why: "Enum variants are accessed with `::`, the path separator." },
        { q: "What guarantees does a Rust enum give you?", opts: ["The value is always the first variant", "The value is exactly one variant — enforced by the type system", "All variants must be the same size", "Enums cannot have methods"], a: 1, why: "An enum value is one and only one variant; the compiler uses this for exhaustiveness checking." },
      ],
      takeaways: ["Enum variants can carry data (unit, tuple, or struct style).", "Construct variants with `EnumName::VariantName(...)`.", "An enum value is always exactly one variant — enabling exhaustive `match`."],
    },
    {
      id: "match",
      title: "Pattern Matching",
      tag: "Exhaustive, expressive `match`",
      min: 12,
      xp: 170,
      blocks: [
        { t: "text", html: "<code>match</code> is Rust's super-powered switch. It compares a value against a series of <em>patterns</em> and runs the first arm that fits. The compiler enforces <strong>exhaustiveness</strong> — you must handle every possible case." },
        { t: "code", caption: "match on an enum", code: "enum Coin {\n    Penny,\n    Nickel,\n    Dime,\n    Quarter,\n}\n\nfn value(c: Coin) -> u32 {\n    match c {\n        Coin::Penny   => 1,\n        Coin::Nickel  => 5,\n        Coin::Dime    => 10,\n        Coin::Quarter => 25,\n    }\n}" },
        { t: "text", html: "Patterns can <strong>bind</strong> data from a variant. Use <code>_</code> as a catch-all arm so you don't have to list every case." },
        { t: "play", prompt: "Run to see binding inside match arms and the catch-all `_`.", code: 'enum Msg {\n    Quit,\n    Move { x: i32, y: i32 },\n    Write(String),\n}\n\nfn describe(m: Msg) -> String {\n    match m {\n        Msg::Quit          => String::from("quit"),\n        Msg::Move { x, y } => format!("move to ({x},{y})"),\n        Msg::Write(text)   => format!("write: {text}"),\n    }\n}\n\nfn main() {\n    println!("{}", describe(Msg::Move { x: 3, y: 7 }));\n    println!("{}", describe(Msg::Write(String::from("hello"))));\n    println!("{}", describe(Msg::Quit));\n}', output: "move to (3,7)\nwrite: hello\nquit", need: ["match", "Msg", "format!"] },
        { t: "callout", kind: "warn", html: "If you leave out a variant and don't have a <code>_</code> arm, the compiler refuses to compile: <em>non-exhaustive patterns</em>. This is one of Rust's best safety guarantees — you can't accidentally forget a case." },
        { t: "callout", kind: "tip", html: "Ranges work in patterns too: <code>1..=5 =&gt; \"low\"</code>. And you can guard an arm with <code>if</code>: <code>n if n &lt; 0 =&gt; \"negative\"</code>." },
        { t: "fib", prompt: "Add a catch-all arm to make the match exhaustive.", before: "match x { 1 => \"one\", 2 => \"two\", ", blank: "_", after: " => \"other\" }", answer: "_", hint: "The wildcard pattern that matches anything." },
      ],
      quiz: [
        { q: "What does the Rust compiler do if a `match` is not exhaustive?", opts: ["Silently returns the default value", "Refuses to compile", "Panics at runtime", "Ignores unhandled cases"], a: 1, why: "The compiler enforces exhaustiveness — all patterns must be covered." },
        { q: "What does `_` mean in a match arm?", opts: ["Match the number zero", "Match the string `_`", "A catch-all that ignores the value", "Only used for tuples"], a: 2, why: "`_` is a wildcard that matches anything and discards the value." },
        { q: "Which of these correctly binds data from a tuple variant `Pair(i32, i32)`?", opts: ["Pair => ...", "Pair { a, b } => ...", "Pair(a, b) => ...", "Pair[a][b] => ..."], a: 2, why: "Tuple variants are destructured with `VariantName(binding1, binding2)`." },
      ],
      takeaways: ["`match` is exhaustive — every possible case must be handled.", "Patterns can bind data from enum variants directly.", "`_` is a wildcard catch-all; guard arms with `if condition`."],
    },
    {
      id: "option",
      title: "Option & null safety",
      tag: "Rust's answer to null",
      min: 10,
      xp: 160,
      blocks: [
        { t: "text", html: "Rust has no <code>null</code>. Instead, optional values are represented by the <code>Option&lt;T&gt;</code> enum — it's either <code>Some(value)</code> or <code>None</code>. The type system forces you to handle the absent case before you can use the value." },
        { t: "code", caption: "Option in the standard library", code: "enum Option<T> {\n    Some(T),\n    None,\n}" },
        { t: "text", html: "You can <code>match</code> on an <code>Option</code> to safely extract its value. Two other convenient patterns: <code>if let</code> for single-arm matching, and <code>unwrap_or</code> to supply a default." },
        { t: "play", prompt: "Run to see three ways to handle an Option.", code: 'fn find_first_even(nums: &[i32]) -> Option<i32> {\n    for &n in nums {\n        if n % 2 == 0 {\n            return Some(n);\n        }\n    }\n    None\n}\n\nfn main() {\n    let numbers = [1, 3, 4, 7];\n    let result = find_first_even(&numbers);\n\n    // match\n    match result {\n        Some(v) => println!("Found: {v}"),\n        None    => println!("Not found"),\n    }\n\n    // if let\n    if let Some(v) = find_first_even(&numbers) {\n        println!("if let got: {v}");\n    }\n\n    // unwrap_or\n    let val = find_first_even(&[1, 3, 5]).unwrap_or(0);\n    println!("unwrap_or default: {val}");\n}', output: "Found: 4\nif let got: 4\nunwrap_or default: 0", need: ["Option", "Some", "None", "if let", "unwrap_or"] },
        { t: "callout", kind: "warn", html: "Calling <code>.unwrap()</code> on <code>None</code> panics. Prefer <code>unwrap_or</code>, <code>unwrap_or_else</code>, or a full <code>match</code>. Reserve <code>unwrap()</code> for prototypes or places where <code>None</code> is genuinely impossible." },
        { t: "fib", prompt: "Return `Some` wrapping the value `42`.", before: "fn answer() -> Option<i32> { ", blank: "Some", after: "(42) }", answer: "Some", hint: "The Option variant that holds a value." },
        { t: "callout", kind: "tip", html: "<code>if let Some(x) = maybe { ... }</code> is idiomatic when you only care about the <code>Some</code> branch and want to skip the <code>None</code> case silently. It's syntactic sugar for a one-arm <code>match</code>." },
      ],
      quiz: [
        { q: "What does `Option<T>` represent?", opts: ["An error type", "A value that is either present (`Some`) or absent (`None`)", "A mutable reference", "A generic trait bound"], a: 1, why: "`Option<T>` models optionality — the value is either `Some(T)` or `None`." },
        { q: "What happens when you call `.unwrap()` on `None`?", opts: ["Returns a default value", "Returns 0", "The program panics", "It compiles but does nothing"], a: 2, why: "`.unwrap()` on `None` triggers a panic — prefer `unwrap_or` or a `match` for safety." },
        { q: "Which syntax handles only the `Some` case without a `None` arm?", opts: ["while let", "if let", "match Some", "for let"], a: 1, why: "`if let Some(x) = opt { ... }` runs the block only when the value is `Some`." },
      ],
      takeaways: ["`Option<T>` replaces null — values are `Some(T)` or `None`.", "Use `match`, `if let`, or `unwrap_or` to safely extract the value.", "`.unwrap()` panics on `None`; prefer safer alternatives in production code."],
    },
  ],
});
